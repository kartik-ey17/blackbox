# BlackBox Backend Configuration
PORT=8000
ENVIRONMENT=development
CORS_ORIGINS="http://localhost:3000,http://localhost:5173,https://*.vercel.app"

# Optional database URL when transitioning from in-memory repository to PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/blackbox"
